{
    "version": 2,
    "name": "oagms",
    "builds": [
       { "src": "./backend/server.js", "use": "@vercel/node" }
    ],
    "routes": [
       { "src": "./backend/(.*)", "dest": "./backend/server.js" }
    ]
 }